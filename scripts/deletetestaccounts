#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$REPO_ROOT"

# Run the interactive test-account deletion tool
npx --yes tsx scripts/delete-all-test-accounts.ts

